# 安全策略

## 报告漏洞

如果你发现了安全漏洞，请不要在公开的 Issue 中报告。请通过以下方式私下联系：

- 在 GitHub 上创建一个私有安全报告（Security Advisory）

## 注意事项

- **JWT_SECRET**：生产环境务必修改 `.env` 中的 `JWT_SECRET` 为随机字符串
- **端口暴露**：如需公网访问，建议使用反向代理（Nginx/Caddy）并启用 HTTPS
- **文件上传**：漫画编辑功能支持上传图片，已在服务端限制文件大小为 50MB

## 依赖更新

定期运行以下命令检查依赖更新：

```bash
pnpm audit
```